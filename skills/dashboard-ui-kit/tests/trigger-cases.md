# Trigger Cases

## Should trigger
- "Add a new right-side modal for step retries with fields and action buttons that match the dashboard style."
- "Create a card list section in the Run panel using the same spacing and separators as the current UI kit."
- "Build a new settings form with switches, helper text, and inline warnings consistent with existing panels."
- "Add a new dashboard page section and make it follow `UIKIT.md` exactly."

## Should NOT trigger
- "Add a new endpoint to `server/index.ts` for provider token refresh."
- "Refactor `server/flowBuilder.ts` prompt-building logic."
- "Fix a cron scheduler bug in backend run mode handling."
- "Create a completely new marketing landing page with a brand-new visual language."

